- LocalScript - Sistema de texto flotante apilado con fade out

local player = game.Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

-- ScreenGui
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "TextOverlayGui"
screenGui.ResetOnSpawn = false
screenGui.Parent = playerGui

-- Frame contenedor del texto (centro-arriba, crece hacia abajo)
local textFrame = Instance.new("Frame")
textFrame.Size = UDim2.new(0.5, 0, 0.35, 0)
textFrame.Position = UDim2.new(0.25, 0, 0.08, 0) -- arriba al centro
textFrame.BackgroundTransparency = 1
textFrame.ClipsDescendants = false
textFrame.Parent = screenGui

-- UIListLayout: apila textos hacia abajo, centrado
local listLayout = Instance.new("UIListLayout")
listLayout.Parent = textFrame
listLayout.SortOrder = Enum.SortOrder.LayoutOrder
listLayout.VerticalAlignment = Enum.VerticalAlignment.Top
listLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
listLayout.Padding = UDim.new(0, 2)

-- TextBox (input) abajo en la pantalla
local textBox = Instance.new("TextBox")
textBox.Size = UDim2.new(0.3, 0, 0.055, 0)
textBox.Position = UDim2.new(0.35, 0, 0.88, 0)
textBox.PlaceholderText = "Escribe aqui..."
textBox.ClearTextOnFocus = false
textBox.Text = ""
textBox.Font = Enum.Font.SourceSansBold
textBox.TextSize = 18
textBox.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
textBox.TextColor3 = Color3.fromRGB(255, 255, 255)
textBox.BorderSizePixel = 0
textBox.Parent = screenGui

Instance.new("UICorner", textBox).CornerRadius = UDim.new(0, 8)

local orderCounter = 0

local function createFloatingText(texto)
	orderCounter = orderCounter + 1

	local label = Instance.new("TextLabel")
	label.Size = UDim2.new(1, 0, 0, 30)
	label.BackgroundTransparency = 1
	label.Text = texto
	label.Font = Enum.Font.SourceSansBold
	label.TextSize = 22 -- un poco mÃ¡s chico que antes
	label.TextColor3 = Color3.fromRGB(255, 255, 255)
	label.TextTransparency = 0
	label.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
	label.TextStrokeTransparency = 0
	label.TextXAlignment = Enum.TextXAlignment.Center
	label.RichText = false
	label.LayoutOrder = orderCounter
	label.Parent = textFrame
